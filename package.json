{
  "name": "if-ai-blocks",
  "version": "0.1.0",
  "private": true,
  "description": "Config-driven AI update block architecture skeleton",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "echo 'workspace is type-only in scaffold phase; no JS emit build configured'",
    "typecheck": "npm run --workspaces typecheck",
    "lint": "npm run --workspaces lint"
  }
}
