{
  "name": "if-ai-blocks",
  "version": "0.1.0",
  "private": true,
  "description": "Config-driven AI update block architecture skeleton",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "typecheck": "npm run --workspaces typecheck",
    "lint": "npm run --workspaces lint",
    "test": "node --experimental-strip-types --test tests/**/*.test.ts",
    "mvp": "node --experimental-strip-types packages/core/src/cli.ts"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  },
  "type": "module"
}
